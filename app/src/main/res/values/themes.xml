<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MyApplication" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/verde_cancha</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/verde_neon</item>
        <item name="colorOnSecondary">@color/black_mate</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/black_mate</item>
        <item name="android:windowBackground">@color/black_mate</item>
    </style>

    <style name="CircleImage">
        <item name="cornerSize">50%</item>
    </style>
</resources>
